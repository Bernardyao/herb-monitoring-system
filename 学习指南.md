# 🎓 中药种植监控系统 - 项目学习文档

## ✅ 项目已成功运行！

恭喜！您的项目已经编译成功并运行。现在让我带您深入学习这个项目。

---

## 📋 运行方式

### 方法1：使用编译好的类文件（推荐）

```bash
# 1. 编译项目（只需首次执行）
compile-java.bat

# 2. 运行主程序
run-java.bat
```

### 方法2：使用 Maven（如果已配置）

```bash
mvn clean compile
mvn exec:java
```

---

## 🎯 项目核心学习路径

### 第一步：登录界面（Swing UI 基础）

**目标文件**: `src\main\java\com\herbs\monitoring\ui\LoginFrame.java`

#### 核心知识点：

1. **窗口创建**
```java
public class LoginFrame extends JFrame {
    public LoginFrame() {
        setTitle("中药种植监控系统-登录");
        setSize(420, 220);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLocationRelativeTo(null);  // 居中显示
    }
}
```

2. **布局管理器 - BorderLayout**
```java
setLayout(new BorderLayout());
// 北部：标题
add(title, BorderLayout.NORTH);
// 中部：输入框
add(inputPanel, BorderLayout.CENTER);
// 南部：按钮
add(buttonPanel, BorderLayout.SOUTH);
```

3. **事件处理**
```java
loginButton.addActionListener(event -> attemptLogin());

private void attemptLogin() {
    String username = usernameField.getText().trim();
    String password = new String(passwordField.getPassword());
    
    if (DEFAULT_USERNAME.equals(username) && DEFAULT_PASSWORD.equals(password)) {
        // 登录成功，跳转到主窗口
        dispose();  // 关闭登录窗口
        MainFrame mainFrame = new MainFrame();
        mainFrame.setVisible(true);
    } else {
        JOptionPane.showMessageDialog(this, "账号密码错误");
    }
}
```

#### 💡 动手练习：
- [ ] 修改窗口标题
- [ ] 更改窗口大小
- [ ] 添加"取消"按钮
- [ ] 修改默认账号密码

---

### 第二步：传感器抽象类（设计模式）

**目标文件**: `src\main\java\com\herbs\monitoring\sensor\AbstractSensor.java`

#### 核心知识点：模板方法模式

```java
public abstract class AbstractSensor {
    // 模板方法：定义采集数据的标准流程
    public SensorReading collect() {
        // 步骤1：生成数据（子类实现）
        double value = collectData(random);
        
        // 步骤2：创建读数对象
        SensorReading reading = new SensorReading(...);
        
        // 步骤3：保存到数据库
        DBUtil.persistSensorReading(...);
        
        // 步骤4：通知所有监听器
        callbacks.forEach(callback -> callback.onReading(reading));
        
        return reading;
    }
    
    // 抽象方法：让子类实现具体的数据生成逻辑
    protected abstract double collectData(Random random);
    protected abstract String dataTypeKey();
    public abstract String listenData();
}
```

#### 子类实现示例：

**温度传感器**（TCP协议）
```java
public class TemperatureSensor extends AbstractSensor {
    @Override
    protected double collectData(Random random) {
        return 15 + random.nextInt(21); // 15-35℃
    }
    
    @Override
    public String listenData() {
        return "TCP监听：温度数据已返回";
    }
}
```

**湿度传感器**（UDP协议）
```java
public class HumiditySensor extends AbstractSensor {
    @Override
    protected double collectData(Random random) {
        return 40 + random.nextInt(41); // 40-80%
    }
    
    @Override
    public String listenData() {
        return "UDP监听：湿度数据已返回";
    }
}
```

#### 💡 设计模式优势：
- ✅ **代码复用**：公共逻辑在父类，避免重复
- ✅ **易于扩展**：添加新传感器只需继承并实现3个方法
- ✅ **统一流程**：所有传感器采集流程一致

#### 💡 动手练习：
- [ ] 添加光照传感器（0-10000 lux，HTTP协议）
- [ ] 添加土壤pH传感器（5.5-7.5，TCP协议）

---

### 第三步：多线程实现（Thread vs Runnable）

**目标文件**: 
- `src\main\java\com\herbs\monitoring\sensor\generator\ThreadSensorGenerator.java`
- `src\main\java\com\herbs\monitoring\sensor\generator\RunnableSensorGenerator.java`

#### 方式1：继承 Thread

```java
public class ThreadSensorGenerator extends Thread {
    @Override
    public void run() {
        for (int i = 1; i <= quantity; i++) {
            String sensorId = String.format("%s-%03d", idPrefix, i);
            AbstractSensor sensor = SensorFactory.createByType(typeKey, sensorId, location);
            sensor.collect();  // 采集数据
            sensor.listenData();  // 模拟网络监听
        }
    }
}

// 使用方法
ThreadSensorGenerator gen = new ThreadSensorGenerator("温度传感器", "TEMP", "黄芪区", 5);
gen.start();  // 启动线程
```

#### 方式2：实现 Runnable

```java
public class RunnableSensorGenerator implements Runnable {
    @Override
    public void run() {
        for (int i = 1; i <= quantity; i++) {
            String sensorId = String.format("%s-%03d", idPrefix, i);
            AbstractSensor sensor = SensorFactory.createByType(typeKey, sensorId, location);
            
            // 为每个传感器创建独立工作线程
            Thread worker = new Thread(() -> {
                sensor.collect();
                sensor.listenData();
            }, "SensorWorker-" + sensorId);
            worker.start();
            
            Thread.sleep(3000);  // 每个传感器间隔3秒
        }
    }
}

// 使用方法
RunnableSensorGenerator gen = new RunnableSensorGenerator("湿度传感器", "HUM", "黄芪区", 5);
Thread thread = new Thread(gen);
thread.start();
```

#### 📊 两种方式对比：

| 特性 | Thread 方式 | Runnable 方式 | 推荐度 |
|------|------------|---------------|--------|
| 实现方式 | 继承 Thread | 实现 Runnable | ⭐⭐⭐⭐ |
| 灵活性 | 无法继承其他类 | 可以继承其他类 | ⭐⭐⭐⭐⭐ |
| 资源共享 | 每个线程独立 | 多线程可共享同一对象 | ⭐⭐⭐⭐ |
| 线程池支持 | 不适合 | 完美支持 | ⭐⭐⭐⭐⭐ |

---

### 第四步：主窗口与数据库 CRUD

**目标文件**: `src\main\java\com\herbs\monitoring\ui\MainFrame.java`

#### 界面结构：

```
┌────────────────────────────────────────────────┐
│        中药种植监控系统-主窗口                  │
├────────────────────────────────────────────────┤
│ sensor_id │ sensor_type │ location │ protocol │
│ TEMP-001  │ 温度传感器  │ 黄芪一区 │ TCP      │
│ HUM-001   │ 湿度传感器  │ 黄芪二区 │ UDP      │
├────────────────────────────────────────────────┤
│ [新增] [修改] [删除] [刷新]                     │
│ [选择类型▾] [传感器ID____] [采集传感器数据]    │
│ [启动周期采集] [停止周期采集]                   │
└────────────────────────────────────────────────┘
```

#### CRUD 操作实现：

**1. 查询（Read）**
```java
private void refreshTable() {
    List<SensorRecord> records = sensorService.listSensors();
    tableModel.setSensors(records);  // 更新 JTable
}
```

**2. 新增（Create）**
```java
private void onAddSensor() {
    SensorRecord record = promptSensorDetails(null);
    if (record != null) {
        sensorService.addSensor(record);
        refreshTable();
    }
}
```

**3. 修改（Update）**
```java
private void onEditSensor() {
    int row = sensorTable.getSelectedRow();
    SensorRecord origin = tableModel.getSensorAt(row);
    SensorRecord updated = promptSensorDetails(origin);
    if (updated != null) {
        sensorService.updateSensor(updated);
        refreshTable();
    }
}
```

**4. 删除（Delete）**
```java
private void onDeleteSensor() {
    int row = sensorTable.getSelectedRow();
    SensorRecord record = tableModel.getSensorAt(row);
    int option = JOptionPane.showConfirmDialog(this, 
        "确认删除传感器 " + record.getSensorId() + "?");
    if (option == JOptionPane.YES_OPTION) {
        sensorService.deleteSensor(record.getSensorId());
        refreshTable();
    }
}
```

#### 💡 动手练习：
1. 点击"新增"，添加传感器 `TEMP-002`
2. 选中该行，点击"修改"，更改位置为"当归区"
3. 使用 SQLite 工具查看数据库验证
4. 删除刚才添加的传感器

---

### 第五步：多线程与 UI 交互

#### 核心问题：UI 线程阻塞

❌ **错误做法**（会冻结界面）：
```java
collectButton.addActionListener(event -> {
    sensor.collect();  // 长时间操作直接在 UI 线程
    JOptionPane.showMessageDialog(this, "完成");  // UI 被阻塞
});
```

✅ **正确做法**（使用线程池）：
```java
private final ExecutorService executor = Executors.newCachedThreadPool();

private void collectOnce() {
    String sensorId = sensorIdField.getText().trim();
    String typeKey = (String) sensorTypeCombo.getSelectedItem();
    
    executor.submit(() -> {  // 在后台线程执行
        AbstractSensor sensor = SensorFactory.createByType(typeKey, sensorId, location);
        sensor.addCallback(buildUiCallback());  // 添加回调
        sensor.collect();  // 采集数据
    });
}

private SensorDataCallback buildUiCallback() {
    return reading -> {
        // 切换回 UI 线程更新界面
        SwingUtilities.invokeLater(() -> {
            JOptionPane.showMessageDialog(this, 
                "传感器: " + reading.getSensorId() + "\n" +
                "采集值: " + reading.getValue());
        });
    };
}
```

#### 关键点：
- `executor.submit()`: 后台线程执行长时间任务
- `SwingUtilities.invokeLater()`: 切回 UI 线程更新界面
- `SensorDataCallback`: 回调机制解耦数据采集与 UI 更新

---

### 第六步：周期采集与告警

#### 使用 Timer 实现周期任务

```java
private Timer periodicTimer;

private void startPeriodicCollection() {
    periodicTimer = new Timer(true);  // true = 守护线程
    periodicTimer.scheduleAtFixedRate(new TimerTask() {
        @Override
        public void run() {
            submitSensorCollection(typeKey, sensorId);  // 每5秒采集一次
        }
    }, 0, 5000);  // 初始延迟0ms，间隔5000ms
}

private void stopPeriodicCollection() {
    if (periodicTimer != null) {
        periodicTimer.cancel();
        periodicTimer = null;
    }
}
```

#### 阈值告警系统

**配置文件**: `src\main\resources\herb_threshold.txt`
```properties
temperature=28  # 温度超过28℃告警
humidity=75     # 湿度超过75%告警
```

**告警逻辑**: `ThresholdService.java`
```java
public Optional<String> evaluate(SensorReading reading) {
    String dataKey = reading.getDataTypeKey();
    Double limit = thresholds.get(dataKey);
    
    if (limit != null && reading.getValue() > limit) {
        String message = String.format("%s %s 当前值 %.2f 超过阈值 %.2f",
            reading.getSensorType(),
            reading.getSensorId(),
            reading.getValue(),
            limit);
        return Optional.of(message);
    }
    return Optional.empty();
}
```

#### 💡 测试步骤：
1. 选择传感器类型和 ID
2. 点击"启动周期采集"
3. 观察控制台输出和弹窗
4. 当温度 > 28℃ 或湿度 > 75% 时会弹出告警
5. 点击"停止周期采集"结束

---

## 🗄️ 数据库详解

### 表结构

**1. sensor 表（传感器信息）**
```sql
CREATE TABLE sensor (
    sensor_id TEXT PRIMARY KEY,    -- 传感器ID
    sensor_type TEXT NOT NULL,     -- 类型
    location TEXT NOT NULL,        -- 位置
    protocol TEXT NOT NULL         -- 协议
);
```

**2. sensor_monitor 表（采集数据）**
```sql
CREATE TABLE sensor_monitor (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    sensor_id TEXT NOT NULL,       -- 传感器ID
    data REAL NOT NULL,            -- 采集值
    data_type TEXT NOT NULL,       -- 数据类型
    collect_time TEXT NOT NULL,    -- 采集时间
    protocol TEXT NOT NULL         -- 协议
);
```

### 查看数据

**方式1：使用 SQLite 命令行工具**
```bash
sqlite3 data\herb-monitor.db
.tables
SELECT * FROM sensor;
SELECT * FROM sensor_monitor ORDER BY collect_time DESC LIMIT 10;
.quit
```

**方式2：使用 DB Browser for SQLite**（推荐）
- 下载：https://sqlitebrowser.org/
- 打开 `data\herb-monitor.db`
- 可视化查看和编辑数据

---

## 📚 核心技术总结

### 1. Swing UI 组件
- `JFrame`: 窗口
- `JLabel`: 标签
- `JTextField`: 文本输入框
- `JPasswordField`: 密码输入框
- `JButton`: 按钮
- `JTable`: 表格
- `JComboBox`: 下拉框
- `JOptionPane`: 弹窗对话框

### 2. 布局管理器
- `BorderLayout`: 五区布局（北、南、东、西、中）
- `FlowLayout`: 流式布局（从左到右）
- `GridLayout`: 网格布局

### 3. 多线程
- `Thread`: 继承方式
- `Runnable`: 接口方式
- `ExecutorService`: 线程池
- `Timer`: 定时任务
- `SwingUtilities.invokeLater()`: UI 线程调度

### 4. 设计模式
- **模板方法模式**: `AbstractSensor`
- **工厂模式**: `SensorFactory`
- **观察者模式**: `SensorDataCallback`
- **DAO 模式**: `SensorDao`, `SensorDataDao`

### 5. 数据库
- JDBC 连接
- PreparedStatement 防注入
- 事务管理
- 连接池（可扩展）

---

## 🎯 学习建议

### 第1天：熟悉运行
- 运行程序，体验所有功能
- 尝试登录、CRUD、采集、告警

### 第2天：阅读代码
- 重点阅读 `AbstractSensor`
- 理解模板方法模式

### 第3天：多线程对比
- 对比 `ThreadSensorGenerator` 和 `RunnableSensorGenerator`
- 运行 `demo-java.bat` 观察输出

### 第4天：UI 与数据库
- 研究 `MainFrame` 如何避免 UI 阻塞
- 学习 `SensorDao` 的 CRUD 实现

### 第5天：扩展功能
- 添加新的传感器类型
- 修改告警阈值
- 优化界面布局

---

## 💡 常见问题

**Q1：编译失败？**
- 确保 JDK 17 已安装
- 运行 `java -version` 检查版本

**Q2：界面打不开？**
- 检查控制台错误信息
- 确认 `lib\sqlite-jdbc-3.36.0.3.jar` 存在

**Q3：数据库在哪里？**
- 位置：`data\herb-monitor.db`
- 首次运行自动创建

**Q4：如何重置数据？**
- 删除 `data` 文件夹
- 重新运行程序

**Q5：中文乱码？**
- 已配置 UTF-8 编码
- 如仍有问题，检查控制台编码设置

---

## 🚀 下一步扩展

### 初级扩展
- [ ] 添加光照传感器
- [ ] 修改界面配色
- [ ] 增加历史数据查询功能

### 中级扩展
- [ ] 使用 MySQL 替代 SQLite
- [ ] 添加用户管理（注册、登录、权限）
- [ ] 数据可视化（图表）

### 高级扩展
- [ ] 网络通信（真实 TCP/UDP）
- [ ] WebSocket 实时推送
- [ ] Spring Boot 后端 + Vue 前端

---

## 📖 推荐学习资料

- **Java Swing**: Oracle Swing Tutorial
- **多线程**: 《Java 并发编程实战》
- **设计模式**: 《Head First 设计模式》
- **数据库**: SQLite 官方文档

---

祝您学习愉快！🎉

如有疑问，请仔细阅读代码注释。每个类都有详细的文档说明。
